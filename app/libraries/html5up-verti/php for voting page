how the PHP should look (PSUEDO CODE): 

BEGIN
- create an array from the id column ?? Can we check in the column itself?
- give picture url of id.ideas[id.seen[seen.length] + 1]
- give title of id.ideas[id.seen[seen.length] + 1]
- give description id.ideas[id.seen[seen.length] +1]
- When pressed either next or Spark it! add +1 to number of votes of that idea
- add the id to the seen column
- start over
- When all ideas are shown: show message (just for validation) 
END

<div id="vote">
<?php	
	-- Can I also check if it is in column instead of array?
	if(array_key_exists ($key, $id.seen)==false{
		echo "<img class='ui circular image' src='$image'>"
		echo "<div class ='ui card'>"
		echo "<div class='content'>"
		echo	"<div class='header'>seen.id.$title</div>"
		echo	"<p>description of this idea</p>"
	else{
		show placeholder	
	}
?>
		<div id="button_holder">
			<div class="ui buttons">
				<div class="ui positive button">Spark it!</div>
				<div class="or"></div>
				<div class="ui red right labeled icon button">
				<i class="right arrow icon"></i>
				Next
				</div>
			</div>
		</div>

</div>


